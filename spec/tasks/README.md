# Tasks

| ID | Goal | Status |
| --- | --- | --- |
| T00 | Scaffold + bootstrap + secure store + MaterialNotices + tests | Done (this package) |

Follow-ups: `flutter_nav_kit` (AuthGuard, AutoPageNavigator) is a separate package and a separate chat.
